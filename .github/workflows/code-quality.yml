name: Code Quality

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "17 4 * * 1"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  code-quality:
    uses: ./.github/workflows/reusable-code-quality.yml

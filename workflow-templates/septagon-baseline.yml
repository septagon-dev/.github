name: Repository Baseline

on:
  pull_request:
  push:
    branches:
      - $default-branch

permissions:
  contents: read

jobs:
  baseline:
    uses: septagon-dev/actions/.github/workflows/reusable-repository-baseline.yml@76726702fe8c06559734b68285ff8566b5903782

    with:
      required_paths: |
        README.md
        .github/CODEOWNERS
        .github/dependabot.yml
